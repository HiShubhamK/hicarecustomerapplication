<?xml version="1.0" encoding="utf-8"?>
<!-- In your drawable folder, create custom_checked_radio_button.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer shape representing the border -->
    <item>
        <shape android:shape="oval">
            <solid android:color="@android:color/transparent" />
            <stroke
                android:width="2dp"
                android:color="@color/grey_font" />
            <size
                android:width="30dp"
                android:height="30dp" />
        </shape>
    </item>
    <!-- Inner shape representing the checked color with increased space using padding -->
    <item
        android:bottom="5dp"
        android:left="5dp"
        android:right="5dp"
        android:top="5dp">
        <shape android:shape="oval">
            <solid android:color="@color/colorAccent" />
            <size
                android:width="20dp"
                android:height="20dp" />
        </shape>
    </item>
</layer-list>
